node_modules/
dist/
*.log
.DS_Store
*.zip

# generated build artifact (regenerated by `npm run build` / prepare)
packages/renderer/src/assets.generated.ts

# generated example output — ONLY source diffs and the curated example story
# are tracked. (keeps generated walkthroughs, incl. any real PR diffs, out.)
examples/*
!examples/sample.diff
!examples/rate-limiting.diff
!examples/rate-limiting.json
